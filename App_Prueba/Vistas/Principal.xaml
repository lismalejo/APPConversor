<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App_Prueba.Vistas.Principal"
             NavigationPage.HasNavigationBar="False">

    <StackLayout BackgroundColor="#48E120">
        <Label Text="Conversor"
               FontSize="40"
               FontAttributes="Bold"       
               Margin="0,20,0,0"
               HorizontalOptions="Center"/>

        <Frame VerticalOptions="FillAndExpand"
               Margin="20"
               CornerRadius="20"
               HasShadow="False">

            <StackLayout VerticalOptions="Center" Spacing="20">
                <Image Source="convertir.png"></Image>
                <Label Text="(cm) a (m)"
                       FontAttributes="Bold"
                       FontSize="40"
                       HorizontalOptions="Center"/>

                <Button Text="Iniciar" 
                        TextColor="White"
                        TextTransform="None"
                        BackgroundColor="#804000"
                        CornerRadius="10"
                        FontSize="Medium"
                        FontAttributes="Bold"
                        x:Name="btniniciar"
                        Clicked="btniniciar_Clicked"/>
            </StackLayout>
        </Frame>
    </StackLayout>

</ContentPage>